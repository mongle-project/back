import express from "express";

const router = express.Router();

// TODO: 인증 관련 라우트 추가
// router.post('/login', ...);
// router.post('/register', ...);
// router.post('/logout', ...);

export default router;
