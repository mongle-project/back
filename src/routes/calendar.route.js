import express from "express";

const router = express.Router();

// TODO: 캘린더 이벤트 관련 라우트 추가
// router.get('/', ...);
// router.post('/', ...);
// router.get('/:id', ...);
// router.put('/:id', ...);
// router.delete('/:id', ...);

export default router;
